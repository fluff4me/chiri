#function at with t!* returns t:
	#list!t values
	#int index

	#if index < 0:
		#set index = max(0, values::length - index)

	#set index = min(index, values::length - 1)

	#uint i = 0
	#each values as t value:
		#if i == index:
			#return value

		#set i++
