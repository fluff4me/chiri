#function join with t!* returns string:
	#list!t values
	#string separator ??= "

	#string result = "
	#for var i = 0, i < length(values), set i++:
		#if i > 0:
			#set result = "#{result}#{separator}

		#set result = "#{result}#{at(values, i)}

	#return result
